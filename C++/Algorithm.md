# Algotithm

```cpp
find();

InputIterator find_if (InputIterator first, InputIterator last, UnaryPredicate pred);
//first和last都为迭代器，pred为一元谓词函数，返回zhao'dao'ho

accumulate(v.begin(),v.end(),0);

for_each();

transform();

fill();

unique();

sort();

replace();

back_inserter();

stable_sort();
```

